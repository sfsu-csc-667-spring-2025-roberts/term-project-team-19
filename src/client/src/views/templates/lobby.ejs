<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UNO Game Lobby</title>
    <% if (locals.styles && styles.length > 0) { %>
        <% styles.forEach(function(style) { %>
            <link rel="stylesheet" href="<%= style %>">
        <% }); %>
    <% } %>
</head>
<body>
    <div id="lobby-container">
        <header class="app-header">
            <div class="header-content">
                <h1>UNO Game Lobby</h1>
                <div class="header-actions">
                    <span class="welcome-text" id="welcome-text">Welcome, <%= locals.user?.username || 'Guest' %>!</span> 
                    <!-- TODO: Fix username display -->
                    <button class="icon-button" id="refresh-button">↻</button>
                    <button class="icon-button" id="logout-button">⇥</button>
                </div>
            </div>
        </header>

        <main class="main-content">
            <div class="lobby-container">
                <h2 class="lobby-title">Game Lobby</h2>
                <button class="create-game-button" id="create-game-button">Create New Game</button>
                <div class="games-list" id="games-list">
                    <% if (locals.games && games.length > 0) { %>
                        <% games.forEach(function(game) { %>
                            <div class="game-card" data-game-id="<%= game.id %>">
                                <div class="game-info">
                                    <h3>Game #<%= game.id %></h3>
                                    <p>Players: <%= game.playerCount %>/<%= game.maxPlayers %></p>
                                    <p>Status: <%= game.status %></p>
                                </div>
                                <div class="game-actions">
                                    <% if (game.status === 'waiting' && game.playerCount < game.maxPlayers) { %>
                                        <button class="join-game-button" data-game-id="<%= game.id %>">Join Game</button>
                                    <% } else { %>
                                        <button class="join-game-button" disabled>Game Full</button>
                                    <% } %>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="game-card">
                            <p>No games available. Create one!</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </main>

        <!-- Create Game Dialog -->
        <div class="dialog-overlay" id="create-game-dialog" style="display: none;">
            <div class="dialog">
                <h2>Create New Game</h2>
                <div class="dialog-content">
                    <div class="form-group">
                        <label for="maxPlayers">Max Players</label>
                        <select id="maxPlayers">
                            <% [2, 3, 4].forEach(function(num) { %>
                                <option value="<%= num %>"><%= num %> Players</option>
                            <% }); %>
                        </select>
                    </div>
                </div>
                <div class="dialog-actions">
                    <button id="cancel-create">Cancel</button>
                    <button class="primary-button" id="confirm-create">Create</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Core dependencies -->
    <script type="module" src="/js/game/game.js"></script>
    <script type="module" src="/js/types/game.js"></script>
    
    <!-- Main lobby script -->
    <script type="module">
        import { GameManager } from '/js/game/game.js';
        import { GameStatus } from '/js/types/game.js';
         
        console.log(games);

        class GameLobbyUI {
            constructor() {
                this.gameManager = GameManager.getInstance();
                this.createDialog = document.getElementById('create-game-dialog');
                this.gamesList = document.getElementById('games-list');
                
                this.initialize();
            }

            initialize() {
                
                // Set up button listeners
                document.getElementById('refresh-button').onclick = () => this.gameManager.fetchGames();
                document.getElementById('logout-button').onclick = async () => {
                    await this.auth.logout();
                    window.location.href = '/login.html';
                };
                document.getElementById('create-game-button').onclick = () => {
                    this.createDialog.style.display = 'block';
                };
                document.getElementById('cancel-create').onclick = () => {
                    this.createDialog.style.display = 'none';
                };
                document.getElementById('confirm-create').onclick = async () => {
                    const maxPlayers = Number(document.getElementById('maxPlayers').value);
                    const gameId = await this.gameManager.createGame(maxPlayers);
                    if (gameId) {
                        this.createDialog.style.display = 'none';
                        window.location.href = `/game.html?id=${gameId}`;
                    }
                };
            }

            fetchGames() {
                console.log(this.gameManager.fetchGames());
                return this.gameManager.fetchGames();
            }


            // createGameItem(game) {
            //     const item = document.createElement('div');
            //     item.className = 'list-item';

            //     const gameInfo = document.createElement('div');
            //     gameInfo.className = 'game-info';

            //     const hostTitle = document.createElement('h3');
            //     hostTitle.textContent = `Host: ${game.hostUsername}`;

            //     const gameStatus = document.createElement('p');
            //     gameStatus.textContent = `Players: ${game.playerCount}/${game.maxPlayers} | Status: ${game.status}`;

            //     const joinButton = document.createElement('button');
            //     joinButton.className = 'join-button';
            //     joinButton.textContent = 'Join Game';
            //     joinButton.disabled = game.status !== GameStatus.WAITING || game.playerCount >= game.maxPlayers;
            //     joinButton.onclick = async () => {
            //         if (await this.gameManager.joinGame(game.id)) {
            //             window.location.href = `/game.html?id=${game.id}`;
            //         }
            //     };

            //     gameInfo.append(hostTitle, gameStatus);
            //     item.append(gameInfo, joinButton);

            //     return item;
            // }

            // renderGames() {
            //     const games = this.gameManager.getGames();
            //     console.log(games);
            //     this.gamesList.innerHTML = '';
                
            //     if (games.length === 0) {
            //         const emptyMessage = document.createElement('div');
            //         emptyMessage.className = 'list-item';
            //         emptyMessage.innerHTML = '<p>No games available. Create one!</p>';
            //         this.gamesList.appendChild(emptyMessage);
            //     } else {
            //         games.forEach(game => {
            //             const gameItem = this.createGameItem(game);
            //             this.gamesList.appendChild(gameItem);
            //         });
            //     }
            // }
        }

        // Initialize the lobby UI when the DOM is loaded
        document.addEventListener('DOMContentLoaded', () => {
            new GameLobbyUI();
        });
    </script>
</body>
</html> 